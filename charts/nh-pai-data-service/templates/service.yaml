apiVersion: v1
kind: Service
metadata:
  name: {{ include "nh-pai-data-service.fullname" . }}
  labels:
    {{- include "nh-pai-data-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nh-pai-data-service.selectorLabels" . | nindent 4 }}
