apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nh-pai-data-service.fullname" . }}-test-connection"
  labels:
    {{- include "nh-pai-data-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nh-pai-data-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
