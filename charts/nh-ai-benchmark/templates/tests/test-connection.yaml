apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nh-ai-benchmark.fullname" . }}-test-connection"
  labels:
    {{- include "nh-ai-benchmark.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nh-ai-benchmark.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
