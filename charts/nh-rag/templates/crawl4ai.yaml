{{- if and .Values.websearch.enabled (not .Values.websearch.crawl4ai.url) -}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nh-rag.crawl4ai.fullname" . }}
  labels:
    {{- include "nh-rag.labels" . | nindent 4 }}
    app.kubernetes.io/component: crawl4ai
spec:
  replicas: {{ .Values.websearch.crawl4ai.replicaCount }}
  selector:
    matchLabels:
      {{- include "nh-rag.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: crawl4ai
  template:
    metadata:
      {{- with .Values.websearch.crawl4ai.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "nh-rag.labels" . | nindent 8 }}
        app.kubernetes.io/component: crawl4ai
        {{- with .Values.websearch.crawl4ai.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "nh-rag.serviceAccountName" . }}
      {{- with .Values.websearch.crawl4ai.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: crawl4ai
          {{- with .Values.websearch.crawl4ai.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.websearch.crawl4ai.image.repository }}:{{ .Values.websearch.crawl4ai.image.tag }}"
          imagePullPolicy: {{ .Values.websearch.crawl4ai.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 11235
              protocol: TCP
          {{- if .Values.websearch.crawl4ai.livenessProbe.enabled }}
          livenessProbe:
            {{- toYaml (omit .Values.websearch.crawl4ai.livenessProbe "enabled") | nindent 12 }}
          {{- end }}
          {{- if .Values.websearch.crawl4ai.readinessProbe.enabled }}
          readinessProbe:
            {{- toYaml (omit .Values.websearch.crawl4ai.readinessProbe "enabled") | nindent 12 }}
          {{- end }}
          {{- with .Values.websearch.crawl4ai.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: CRAWL4AI_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "nh-rag.websearch.secretName" . }}
                  key: CRAWL4AI_API_TOKEN
            {{- with .Values.websearch.crawl4ai.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            # RAM-backed emptyDir: k8s has no shm-size knob and Chromium
            # crashes on the 64Mi /dev/shm default
            - name: dshm
              mountPath: /dev/shm
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.websearch.crawl4ai.shmSize }}
      {{- with .Values.websearch.crawl4ai.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.websearch.crawl4ai.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.websearch.crawl4ai.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nh-rag.crawl4ai.fullname" . }}
  labels:
    {{- include "nh-rag.labels" . | nindent 4 }}
    app.kubernetes.io/component: crawl4ai
spec:
  type: {{ .Values.websearch.crawl4ai.service.type }}
  ports:
    - port: {{ .Values.websearch.crawl4ai.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nh-rag.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: crawl4ai
{{- end }}
